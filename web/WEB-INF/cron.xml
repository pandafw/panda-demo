<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
	<cron>
		<url>/task/ping</url>
		<description>Execute every 1 minutes to prevent application sleep</description>
		<schedule>every 1 minutes</schedule>
	</cron>

	<cron>
		<url>/task/filecleanup/start</url>
		<description>Execute every 30 minutes to remove the upload temporary files</description>
		<schedule>every 30 minutes</schedule>
	</cron>

	<cron>
		<url>/task/reset/start</url>
		<description>Execute every 8 hours to reset the database and index</description>
		<schedule>every 8 hours</schedule>
	</cron>
</cronentries>
